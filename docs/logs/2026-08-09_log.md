# 工作日志 2026-08-09

## 目标与实际耗时

目标：恢复后第一天，完成可视化环境排障 + nb01 数据概览四张图。实际耗时约 3 小时，其中近一半花在两个环境问题上，属于计划外但必须解决的阻塞项。

## 今日完成

1. **环境排障（计划外）**

   - seaborn `set_style()` 会覆盖 `rcParams` 的中文字体设置 → 改为在 set_style 时注入字体：`sns.set_style('whitegrid', {'font.sans-serif': ['Microsoft YaHei', 'SimHei']})`

   - PyCharm 2024.1.7 的 matplotlib 后端与 matplotlib ≥3.9 不兼容（tostring_rgb 已移除）→ 放弃降级，改用 .ipynb 内联后端

2. **nb01 数据概览完成**：口径函数 `salary_df()` + 口径自检（n=671，中位 1.15 万，通过）；图0-1 数据漏斗、图0-2 经验/学历分布、图0-3 发布月份、图0-4 字段缺失率，均已保存至 docs/charts/

3. **图表规范升级**：确立「结论式主标题 + 底部灰色口径脚注」标准，封装 `finish_fig()` 统一收尾（标题/脚注/150dpi 保存），后续 notebook 直接复用

## 踩坑记录

- 学历层级列存的是 1-5 数字编码，直接 reindex 中文标签会得到全 0 → 必须先 `map(EDU_MAP)` 再统计。这类「编码 vs 标签」问题在后续技能、公司性质字段上也要警惕。

## 明日计划

nb03 薪资分析（均值/中位数三连证 + 经验×薪资带宽图）+ nb02 城市分布。
